<!DOCTYPE html>
<html>
<head>
    <title></title>
</head>
<body>
    <a href="/restaurants/new">Add a Restaurant</a>
    <a href="/users/new">Add a User</a>
    <a href="/restaurants">See all Restaurants</a>
    <a href="/users">See all Users</a>
    <p>Name: <%=@user.name%><br></p>
    <p>Restaurant(s) <%=@user.name%> wants to try:</p>

    <ul>
        <%@user.restaurants.each do |restaurant|%>
            <li><%=restaurant.name%></li>
             <form action="/users/<%=@user.id%>/remove_try" method="POST">
                 <input type="hidden" name="restaurant_id" value="<%=restaurant.id%>">
                 <input type="submit" value="delete">
             </form>
        <%end%>
    </ul>

    <p><%=@user.name%>'s Favorite Restaurants:</p>
    <ul>
        <%@user.favorite_restaurants.each do |restaurant|%>
            <li><%=restaurant.name%></li>
            <form action="/users/<%=@user.id%>/remove_favorite" method="POST">
                 <input type="hidden" name="restaurant_id" value="<%=restaurant.id%>">
                 <input type="submit" value="delete">
             </form>
        <%end%>
    </ul>


    <form action="/users/<%=@user.id%>/add_restaurant" method="POST">
        <select name="restaurant_id" type="text">
            <%Restaurant.all.each do |restaurant|%>
                <option value="<%=restaurant.id%>"><%=restaurant.name%></option>
            <%end%>
        </select>
        <input type="checkbox" name="try" value="try">Try
        <input type="checkbox" name="favorite" value="favorite">Favorite
        <input type="submit" name="submit">
    </form>


</body>
</html>
